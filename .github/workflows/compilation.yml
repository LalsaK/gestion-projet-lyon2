name: Compilation LaTeX
on: [push]
jobs:
  build_latex:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout du code
        uses: actions/checkout@v3
      - name: Compilation du PDF
        uses: xu-cheng/latex-action@v3
        with:
          root_file: rapport.tex